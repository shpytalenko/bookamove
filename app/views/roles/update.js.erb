<% if not params[:role][:active] == "1" or not params[:role][:mailbox] == "1" %>
var user = $('.user-name').data('user');
var subtask_id = $('#subtask_id').val();

localStorage.removeItem(user + '_mailRecordStaticLink_' + subtask_id);
<% end %>

location.href = "/roles";